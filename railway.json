{
    "$schema": "https://railway.app/railway.schema.json",
    "deploy": {
        "preDeployCommand": "mkdir -p storage/app/public storage/app/private && php artisan storage:link --force && php artisan migrate --force && php artisan db:seed --force"
    }
}
